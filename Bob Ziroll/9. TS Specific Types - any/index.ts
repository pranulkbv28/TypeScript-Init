let value: any = 1;
value = "Hello";
value.toUpperCase();

/*
One legitimate use-case: you're in the process of
transitioning a code base from JavaScript to
TypeScript and don't immediately have the time to
write all your complex types, so you just need a way
to "get around TypeScript" for now.


This is nothng but a temporary use case*/
